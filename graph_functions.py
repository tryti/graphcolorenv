def chromatic_number(g):
    """
    Returns the chromatic_number for the graph g.
    Calculated using a brute force algorithm.
    Args:
        g (NetworkX.Graph) - network x graph
    Returns:
        cn (int) - chromatic_number for g
    """
